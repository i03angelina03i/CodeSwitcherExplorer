const format = (value) => Number.isInteger(value) ? value : value.toFixed(1)

export default function Statistics({ statistics }) {
  return <section className="stats-panel"><div className="section-heading"><div><p className="eyebrow">02 · Überblick</p><h2>Analyseübersicht</h2></div><span className="live-label">LIVE</span></div><div className="stat-grid"><div className="stat-card neutral"><span>Wörter insgesamt</span><strong>{statistics.total}</strong></div><div className="stat-card german"><span>Deutsch</span><strong>{statistics.de || 0}</strong></div><div className="stat-card russian"><span>Russisch</span><strong>{statistics.ru || 0}</strong></div><div className="stat-card switch"><span>Code-Switches</span><strong>{statistics.switches}</strong></div></div><div className="percentage-row"><div><span><i className="legend-dot german" /> Deutsch</span><strong>{(statistics.languagePercents?.de || 0).toFixed(1)}%</strong></div><div><span><i className="legend-dot russian" /> Russisch</span><strong>{(statistics.languagePercents?.ru || 0).toFixed(1)}%</strong></div></div>{statistics.languages.length > 0 && <div className="sequence-summary"><div><span>Erkannte Sprachen</span><strong>{statistics.languages.join(' · ')}</strong></div><div><span>Lexikalische Diversität</span><strong>{statistics.total ? (statistics.uniqueWords / statistics.total * 100).toFixed(1) : 0}%</strong></div></div>}</section>
}
