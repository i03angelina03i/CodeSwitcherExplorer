export default function TextInput({ value, onChange, onAnalyze, onLoadExample, onClear }) {
  return <section className="input-panel panel"><div className="section-heading"><div><p className="eyebrow">01 · Korpus</p><h2>Text untersuchen</h2></div><span className="mono-label">DE / RU</span></div><textarea value={value} onChange={(event) => onChange(event.target.value)} placeholder="Fügen Sie hier eine Audiotranskription oder einen beliebigen Text ein..." aria-label="Text zur Analyse" /><div className="input-actions"><button className="primary-button" onClick={onAnalyze}>Analyse starten <span>↗</span></button><button className="text-button" onClick={onLoadExample}>Beispiel laden</button><button className="text-button" onClick={onClear}>Text löschen</button></div></section>
}
